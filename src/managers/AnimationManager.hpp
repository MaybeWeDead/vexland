#pragma once

// -----------------------------------------------------------------------
// AnimationManager.hpp — двигает SWindowAnimState.cur* к goal* на каждый
// tick, вызывает CWindow::applyCurrentGeometryToX11() чтобы применить
// промежуточный кадр к реальному X11 окну. Восстановлено по контракту,
// описанному в комментариях Window.cpp (setGoalGeometry/
// applyCurrentGeometryToX11 уже ссылались на "AnimationManager_v1",
// который не попал в текущий архив — здесь реализация с нуля под тот
// же контракт: cur/goal/begin в SWindowAnimState, animating flag).
//
// Вызывается из eventLoop() в main.cpp на каждой итерации цикла — САМ
// решает, пора ли обновлять кадр (throttle по времени, не чаще целевого
// FPS), так что вызывающему коду не нужно думать о частоте.
// -----------------------------------------------------------------------

namespace AnimationManager {

// Продвигает анимации всех окон на один шаг, если прошло достаточно
// времени с последнего кадра (внутренний троттлинг по target FPS).
// Дешёвый вызов, если анимировать нечего — можно звать на каждой
// итерации eventLoop без опасений за CPU.
void tick();

} // namespace AnimationManager
